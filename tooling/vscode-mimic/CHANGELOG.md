# Changelog

## 0.1.0 — initial release

- Right-click any image in the Explorer to generate Flutter, HTML, or React code via `mimic`.
- Command palette entries for all targets.
- Progress notification while the vision model is running.
- `mimic doctor` command surfaces in the VS Code output channel.
- Settings: binary path, default target, default provider, output directory.
